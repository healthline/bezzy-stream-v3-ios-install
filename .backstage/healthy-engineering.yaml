apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: bezzy-stream-v3-ios-install
  description: Static OTA install page for Bezzy Stream v3 iOS test builds
spec:
  type: service # can be one of: service, library, website, iac-pipeline
  lifecycle: production # can be one of: experimental, production, deprecated
  owner: team-cje-authx # your existing tech team in Backstage, i.e. team-developer-platforms